# v0.3 Chrome 插件 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Chrome MV3 插件,双模式(本地全功能编辑 / 远程只读批注),side panel + content script + IndexedDB。

**Architecture:** content script 注入当前页(复用 v0.2 text-quote/sanitize/toolbar,改 iDoc→document);side panel 批注卡片(静态排序)+ 回灌;background 安装/activeTab;IndexedDB 存储。

**Tech Stack:** Chrome MV3, Vanilla JS, IndexedDB, DOMPurify(vendor), text-quote anchoring(v0.2 复用)。

## Global Constraints
- 工作目录:`htmlGenius/extension/`(新子目录,与 v0.2 web app 共存)。
- 分支:`v0.2-html-editing`(继续迭代)。
- 复用 v0.2 `static/anchoring/text-quote.js` + `static/vendor/purify.min.js`(copy 到 extension/)。
- 权限:`activeTab` + `sidePanel`(不申请 `<all_urls>`)。
- 双模式:本地(file/localhost/data/blob)默认编辑;远程默认只读批注。
- commit message 中文。

---

### Task 1: 插件骨架(manifest + background + 目录)

**Files:** `extension/manifest.json`, `extension/background.js`, `extension/icons/`

- [ ] **Step 1: 建目录 + manifest**
创建 `extension/manifest.json`:
```json
{
  "manifest_version": 3,
  "name": "htmlGenius",
  "version": "0.3.0",
  "description": "HTML 批注 + 编辑 + 回灌",
  "permissions": ["activeTab", "sidePanel", "storage"],
  "side_panel": { "default_path": "sidepanel.html" },
  "background": { "service_worker": "background.js" },
  "action": { "default_title": "htmlGenius" }
}
```
- [ ] **Step 2: background.js**
```javascript
// 点击图标 → 打开 side panel
chrome.runtime.onInstalled.addListener(() => {
  chrome.sidePanel.setPanelBehavior({ openPanelOnActionClick: true });
});
```
- [ ] **Step 3: 占位 sidepanel.html + content-script.js**
```html
<!-- extension/sidepanel.html -->
<!DOCTYPE html><html><body>
<div id="app">加载中…</div>
<script src="sidepanel.js"></script>
</body></html>
```
```javascript
// extension/content-script.js(占位)
console.log("htmlGenius content script loaded");
```
- [ ] **Step 4: copy 复用文件**
```bash
cp static/anchoring/text-quote.js extension/text-quote.js
cp static/vendor/purify.min.js extension/vendor/purify.min.js
```
manifest 加 `content_scripts`:
```json
"content_scripts": [{
  "matches": ["<all_urls>"],
  "js": ["vendor/purify.min.js", "text-quote.js", "storage.js", "content-script.js"],
  "run_at": "document_idle"
}]
```
- [ ] **Step 5: 加载测试**
Chrome → `chrome://extensions` → 开发者模式 → 加载 `extension/` → 打开任意网页 → 控制台应有 "htmlGenius content script loaded"。点图标 → side panel 打开。
- [ ] **Step 6: commit**
```bash
git add extension/ && git commit -m "feat(v0.3-T1): Chrome插件骨架(manifest+background+side panel+content script)"
```

### Task 2: IndexedDB 存储层

**Files:** `extension/storage.js`

- [ ] **Step 1: 写 storage.js**
```javascript
// extension/storage.js — IndexedDB 封装(annotations + versions)
const DB_NAME = "htmlgenius";
const DB_VERSION = 1;

function openDB() {
  return new Promise((resolve, reject) => {
    const req = indexedDB.open(DB_NAME, DB_VERSION);
    req.onupgradeneeded = (e) => {
      const db = e.target.result;
      if (!db.objectStoreNames.contains("annotations")) {
        const s = db.createObjectStore("annotations", { keyPath: "id" });
        s.createIndex("document_id", "document_id", { unique: false });
      }
      if (!db.objectStoreNames.contains("versions")) {
        const v = db.createObjectStore("versions", { keyPath: "id", autoIncrement: true });
        v.createIndex("document_id", "document_id", { unique: false });
      }
    };
    req.onsuccess = () => resolve(req.result);
    req.onerror = () => reject(req.error);
  });
}

async function dbPut(store, value) {
  const db = await openDB();
  return new Promise((res, rej) => {
    const tx = db.transaction(store, "readwrite");
    tx.objectStore(store).put(value);
    tx.oncomplete = () => res(value);
    tx.onerror = () => rej(tx.error);
  });
}

async function dbGetAllByIndex(store, indexName, value) {
  const db = await openDB();
  return new Promise((res, rej) => {
    const tx = db.transaction(store, "readonly");
    const req = tx.objectStore(store).index(indexName).getAll(value);
    req.onsuccess = () => res(req.result);
    req.onerror = () => rej(req.error);
  });
}

async function dbDelete(store, key) {
  const db = await openDB();
  return new Promise((res, rej) => {
    const tx = db.transaction(store, "readwrite");
    tx.objectStore(store).delete(key);
    tx.oncomplete = () => res(true);
    tx.onerror = () => rej(tx.error);
  });
}

// 批注 CRUD
const Storage = {
  async getDocumentId() {
    return location.origin + location.pathname;
  },
  async saveAnnotation(ann) {
    ann.id = ann.id || "ann_" + Date.now() + "_" + Math.random().toString(36).slice(2, 8);
    ann.document_id = await this.getDocumentId();
    ann.created_at = ann.created_at || new Date().toISOString();
    ann.status = ann.status || "open";
    return dbPut("annotations", ann);
  },
  async listAnnotations(docId) {
    return dbGetAllByIndex("annotations", "document_id", docId);
  },
  async deleteAnnotation(id) {
    return dbDelete("annotations", id);
  },
  async saveVersion(docId, html) {
    return dbPut("versions", { document_id: docId, html_content: html, created_at: new Date().toISOString(), source: "edit" });
  },
  async listVersions(docId) {
    return dbGetAllByIndex("versions", "document_id", docId);
  },
};
```
- [ ] **Step 2: 测试**
在 Chrome 控制台:
```javascript
await Storage.saveAnnotation({ selector: { exact: "test" }, quote: "test", body: { comment: "hi" } });
const docId = await Storage.getDocumentId();
const list = await Storage.listAnnotations(docId);
console.assert(list.length >= 1, "应有批注");
```
- [ ] **Step 3: commit**

### Task 3: 双模式判断 + content script 核心

**Files:** `extension/content-script.js`

- [ ] **Step 1: 双模式判断 + 初始化**
```javascript
// extension/content-script.js
const { describe, anchor } = await import(chrome.runtime.getURL("text-quote.js"));

const isLocal = ["file:", "data:", "blob:"].includes(location.protocol)
  || ["localhost", "127.0.0.1", "0.0.0.0"].includes(location.hostname);

let editMode = isLocal; // 本地默认可编辑;远程默认只读

// 注入样式
const style = document.createElement("style");
style.textContent = `
  .hg-hl{ position:fixed; background:rgba(255,243,160,0.55); pointer-events:none; z-index:2147483646; }
  .hg-hl.flash{ background:rgba(251,191,36,0.75); }
  #hg-toolbar{ position:fixed; z-index:2147483647; display:none; background:#1f2328; color:#fff; border-radius:6px; padding:4px; gap:4px; }
  #hg-toolbar.show{ display:flex; }
  #hg-toolbar button{ background:transparent; color:#fff; border:0; cursor:pointer; padding:2px 8px; border-radius:4px; font-size:13px; }
  #hg-toolbar button:hover{ background:#374151; }
  #hg-toolbar .hg-sep{ width:1px; height:18px; background:#4b5563; margin:0 2px; }
`;
document.head.appendChild(style);

// 浮工具栏
const toolbar = document.createElement("div");
toolbar.id = "hg-toolbar";
toolbar.innerHTML = `<button data-act="comment">批注</button>`;
if (isLocal) {
  toolbar.innerHTML += `<span class="hg-sep"></span><button data-act="bold"><b>B</b></button><input type="color" data-act="color" style="width:24px;height:24px;">`;
}
document.body.appendChild(toolbar);
toolbar.addEventListener("mousedown", (e) => e.preventDefault());

// 连接 side panel
let port = null;
function connectPanel() {
  port = chrome.runtime.connect({ name: "content" });
  port.onMessage.addListener(handlePanelMessage);
}
connectPanel();

// ... (Task 4 续:批注创建 + overlay + toolbar 交互)
```
- [ ] **Step 2: commit**

### Task 4: 批注创建 + overlay 高亮 + 通信

**Files:** `extension/content-script.js`(续)

- [ ] **Step 1: selectionchange → toolbar 定位 + 批注创建**
```javascript
// 续 content-script.js

let barRAF = 0;
document.addEventListener("selectionchange", () => {
  if (barRAF) return;
  barRAF = requestAnimationFrame(() => {
    barRAF = 0;
    const sel = document.getSelection();
    if (!sel || sel.isCollapsed || sel.rangeCount === 0) { toolbar.classList.remove("show"); return; }
    const rect = sel.getRangeAt(0).getBoundingClientRect();
    if (rect.width === 0 && rect.height === 0) { toolbar.classList.remove("show"); return; }
    toolbar.style.left = (rect.left + rect.width / 2) + "px";
    toolbar.style.top = (rect.top - 8) + "px";
    toolbar.style.transform = "translate(-50%,-100%)";
    toolbar.classList.add("show");
  });
});

toolbar.addEventListener("click", async (e) => {
  const btn = e.target.closest("button[data-act]");
  if (!btn) return;
  if (btn.dataset.act === "comment") {
    toolbar.classList.remove("show");
    await createAnnotation();
  } else if (btn.dataset.act === "bold") {
    applyStyle("fontWeight", "bold");
  }
});
toolbar.querySelector('input[data-act="color"]')?.addEventListener("input", (e) => {
  applyStyle("color", e.target.value);
});

async function createAnnotation() {
  const sel = document.getSelection();
  if (!sel || sel.rangeCount === 0) return;
  const range = sel.getRangeAt(0);
  const selector = describe(range, document.body);
  if (!selector?.exact) return;
  const comment = prompt("批注评论(可空):") || "";
  const docId = await Storage.getDocumentId();
  await Storage.saveAnnotation({
    document_id: docId, selector, quote: selector.exact,
    body: { comment, action: "rewrite", instruction: "" },
  });
  sel.removeAllRanges();
  await loadAnnotations();
  sendToPanel({ type: "annotations-updated" });
}

function applyStyle(prop, value) {
  const sel = document.getSelection();
  if (!sel?.rangeCount) return;
  const range = sel.getRangeAt(0);
  const span = document.createElement("span");
  span.style[prop] = value;
  try { range.surroundContents(span); }
  catch (e) { span.appendChild(range.extractContents()); range.insertNode(span); }
}
```
- [ ] **Step 2: overlay 加载/渲染 + position:fixed**
```javascript
const overlays = [];
async function loadAnnotations() {
  // 清旧
  overlays.forEach((o) => o.remove());
  overlays.length = 0;
  const docId = await Storage.getDocumentId();
  const items = await Storage.listAnnotations(docId);
  for (const ann of items) {
    const range = anchor(ann.selector, document.body);
    if (range) {
      ann._status = "open";
      const rects = range.getClientRects();
      for (const r of rects) {
        const div = document.createElement("div");
        div.className = "hg-hl";
        div.style.cssText = `left:${r.left}px;top:${r.top}px;width:${r.width}px;height:${r.height}px;`;
        document.body.appendChild(div);
        overlays.push(div);
      }
    } else {
      ann._status = "stale";
    }
  }
  // 存当前批注列表供 panel
  window.__hgAnnotations = items;
  sendToPanel({ type: "annotations-updated" });
}

// 滚动/resize → 更新 overlay 位置(rAF)
let updateRAF = 0;
function updateOverlayPositions() {
  if (updateRAF) return;
  updateRAF = requestAnimationFrame(() => {
    updateRAF = 0;
    // 简化:重新 loadAnnotations(重 anchor + 重定位)
    loadAnnotations();
  });
}
window.addEventListener("scroll", updateOverlayPositions, true);
window.addEventListener("resize", updateOverlayPositions);
```
- [ ] **Step 3: 通信(content → panel)**
```javascript
function sendToPanel(msg) {
  if (port) port.postMessage(msg);
}
function handlePanelMessage(msg) {
  if (msg.type === "get-annotations") {
    sendToPanel({ type: "annotations-list", items: window.__hgAnnotations || [], isLocal });
  } else if (msg.type === "scroll-to") {
    // panel 点卡片 → 滚到批注
    const items = window.__hgAnnotations || [];
    const ann = items.find((a) => a.id === msg.id);
    if (ann) {
      const range = anchor(ann.selector, document.body);
      if (range) {
        range.startContainer.parentElement?.scrollIntoView({ behavior: "smooth", block: "center" });
      }
    }
  } else if (msg.type === "enable-edit") {
    document.body.contentEditable = "true";
  } else if (msg.type === "get-export") {
    const items = window.__hgAnnotations || [];
    sendToPanel({ type: "export-data", items });
  }
}

// 初始化
loadAnnotations();
if (isLocal) {
  document.body.contentEditable = "true";
}
```
- [ ] **Step 4: 手动测试**
加载插件 → 打开 file:// 的 HTML → 选词 → 浮栏 → 批注 → overlay 高亮。打开远程网页 → 默认只读(不 contenteditable)。
- [ ] **Step 5: commit**

### Task 5: side panel(卡片 + 回灌 + 编辑入口)

**Files:** `extension/sidepanel.html`, `extension/sidepanel.js`, `extension/sidepanel.css`

- [ ] **Step 1: sidepanel.html + css**
```html
<!DOCTYPE html><html lang="zh-CN"><head>
<meta charset="UTF-8"><link rel="stylesheet" href="sidepanel.css">
</head><body>
<div id="mode-indicator"></div>
<div id="annotations"></div>
<div id="actions">
  <button id="export-btn">回灌</button>
  <button id="edit-btn" hidden>编辑</button>
</div>
<script src="sidepanel.js"></script>
</body></html>
```
```css
body { font-family: -apple-system, system-ui, sans-serif; margin: 0; padding: 8px; font-size: 13px; }
#mode-indicator { padding: 4px 8px; border-radius: 4px; margin-bottom: 8px; font-weight: 600; }
#mode-indicator.local { background: #eef2ff; color: #4338ca; }
#mode-indicator.remote { background: #fef3c7; color: #92400e; }
.card { background: #fff; border: 1px solid #e5e7eb; border-radius: 6px; padding: 8px; margin-bottom: 6px; }
.card .quote { color: #6b7280; font-size: 12px; border-left: 3px solid #d1d5db; padding-left: 4px; margin-bottom: 4px; }
.card.stale { opacity: 0.5; }
#actions { margin-top: 8px; display: flex; gap: 4px; }
button { padding: 4px 12px; border: 0; border-radius: 4px; cursor: pointer; font-size: 12px; }
#export-btn { background: #0969da; color: #fff; }
#edit-btn { background: #f59e0b; color: #fff; }
```
- [ ] **Step 2: sidepanel.js**
```javascript
let port = chrome.runtime.connect({ name: "panel" });
let isLocal = false;

port.onMessage.addListener((msg) => {
  if (msg.type === "annotations-list") {
    isLocal = msg.isLocal;
    renderMode();
    renderCards(msg.items);
  } else if (msg.type === "annotations-updated") {
    port.postMessage({ type: "get-annotations" });
  } else if (msg.type === "export-data") {
    exportPrompt(msg.items);
  }
});

function renderMode() {
  const el = document.getElementById("mode-indicator");
  el.textContent = isLocal ? "📍 本地文档(可编辑)" : "🌐 远程网页(只读批注)";
  el.className = isLocal ? "local" : "remote";
  document.getElementById("edit-btn").hidden = isLocal;
}

function renderCards(items) {
  const c = document.getElementById("annotations");
  c.innerHTML = "";
  if (!items?.length) {
    c.innerHTML = '<div style="color:#9ca3af;text-align:center;padding:16px;">选中文字 → 点「批注」</div>';
    return;
  }
  for (const ann of items) {
    const card = document.createElement("div");
    card.className = "card" + (ann._status === "stale" ? " stale" : "");
    card.innerHTML = `<div class="quote">${(ann.quote||"").slice(0,60)}</div><div>${ann.body?.comment||"(无)"}</div>`;
    card.addEventListener("click", () => port.postMessage({ type: "scroll-to", id: ann.id }));
    c.appendChild(card);
  }
}

document.getElementById("export-btn").addEventListener("click", () => {
  port.postMessage({ type: "get-export" });
});

document.getElementById("edit-btn").addEventListener("click", () => {
  if (confirm("⚠ 编辑仅本地临时修改,刷新或关闭页面后丢失,无法保存回原网页。\n\n确认进入编辑模式?")) {
    port.postMessage({ type: "enable-edit" });
    document.getElementById("edit-btn").hidden = true;
    document.getElementById("mode-indicator").textContent = "📍 编辑模式(临时)";
  }
});

// 初始化:请求批注列表
port.postMessage({ type: "get-annotations" });

function exportPrompt(items) {
  const lines = items.map((a, i) => {
    const sel = a.selector || {};
    const exact = sel.exact || a.quote || "";
    const prefix = (sel.prefix || "").trim();
    const suffix = (sel.suffix || "").trim();
    const loc = (prefix || suffix)
      ? `定位:${prefix ? `前文「${prefix}」` : ""}【原文】「${exact}」${suffix ? `后文「${suffix}」` : ""}`
      : `定位:【原文】「${exact}」`;
    return `==批注${i+1}==\n${loc}\n评论:${a.body?.comment || "(无)"}`;
  });
  const prompt = `请基于以下批注逐条修改,输出完整 HTML:\n\n${lines.join("\n\n")}`;
  navigator.clipboard.writeText(prompt).then(() => {
    document.getElementById("export-btn").textContent = "已复制 ✓";
    setTimeout(() => document.getElementById("export-btn").textContent = "回灌", 1500);
  });
}
```
- [ ] **Step 3: 测试**
加载插件 → 打开网页 → 批注 → side panel 显示卡片。本地 → "可编辑";远程 → "只读" + 编辑按钮(点击弹提示)。回灌 → 复制 prompt。
- [ ] **Step 4: commit**

### Task 6: 集成测试 + push

- [ ] **Step 1: 完整流程测试**
  - file:// 打开 HTML → 默认编辑 → 改字 → 选词 → 批注 → side panel 卡片 → 回灌。
  - 远程网页(如 https://example.com)→ 默认只读 → 选词 → 批注 → 点编辑 → 弹提示 → 确认 → 可编辑。
  - 关闭/重开 side panel → 批注仍在(IndexedDB)。
- [ ] **Step 2: push**
```bash
git add extension/ && git commit -m "feat(v0.3): Chrome插件完整实现(双模式+side panel+批注+回灌)" && git push
```

---

## Self-Review

**Spec 覆盖**:
- G1 MV3 插件 → Task 1(manifest/background) ✅
- G2 双模式 → Task 3(isLocal 判断 + editMode) ✅
- G3 批注+回灌 → Task 4(批注) + Task 5(回灌) ✅
- G4 复用核心 → Task 1(text-quote/DOMPurify copy) ✅
- review 修订:overlay position:fixed(Task 4)✅;静态排序卡片(Task 5)✅;activeTab(Task 1)✅;port 通信(Task 4/5)✅;IndexedDB(Task 2)✅;远程编辑入口+提示(Task 5)✅
