# v0.5 飞书 OAuth 登录 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用飞书 OAuth + 服务端 session token 替换 team-token 鉴权，让协同批注的 author 身份变成飞书校验过的硬身份（`open_id`），并开放协同入口。

**Architecture:** 后端新增 sessions 表 + lark OpenAPI 客户端 + session 鉴权依赖；`require_team*` → `require_session*`；批注端点的 author/team_id 全部从 session 注入（删掉 `X-User-Id`/`X-User-Name` 头）。SSE/presence/rooms/批注模型与表结构**不改**。扩展侧栏加「飞书登录」按钮走 `chrome.identity.launchWebAuthFlow`，登录后存 session_token，`remote-store`/`sync` 改用它。`state` 用 HMAC 自签（无状态、5min TTL）。

**Tech Stack:** FastAPI + SQLite(WAL) + pydantic v2；Python 3.9（`from __future__ import annotations`，`Optional[]` 而非 `|`）；httpx（提升为运行时依赖，用于调飞书）；Chrome MV3（`chrome.identity.launchWebAuthFlow`）；pytest + playwright。

**Spec:** `docs/2026-07-06-v0.5-lark-oauth-design.md`

## Global Constraints

- Python 3.9：类型用 `Optional[X]` 不用 `X | None`；异步函数 `async def`。
- commit message：中文 + conventional 前缀（`feat(v0.5-Tn):` / `fix(v0.5):` / `test(v0.5):` / `docs(v0.5):`）。
- git 作者已 `git config --local` 为 `deuce.chen <271208123+chenerzhong123-cloud@users.noreply.github.com>`。
- 分支：`v0.5-lark-oauth`（已 rebase 到 main）。**不合并 main**，用户验证后再合。
- SSE/presence/rooms/批注 schema 与 `storage.delete_annotation(aid, team_id, actor_id)` 签名**保持不变**。
- `HG_TEAMS` 整体下线；新增 env：`HG_LARK_APP_ID`、`HG_LARK_APP_SECRET`、`HG_DEFAULT_TEAM`（缺省 `"default"`）、`HG_AUTH_ALLOW_DEV`（缺省 `"0"`）、`HG_SESSION_TTL`（缺省 `604800`）、`HG_LARK_BASE`（缺省 `https://open.feishu.cn`）。
- 测试：`uv run pytest -v` 全绿；新增/更新测试覆盖 sessions / lark / auth 依赖 / 鉴权端点 / 扩展客户端。

---

## File Structure

**新增**
- `server/sessions.py` — sessions 表的 CRUD（create/get/delete/prune），复用 `storage._connect`。
- `server/lark.py` — 飞书 OpenAPI 客户端（authorize_url / app_access_token / exchange_code），httpx 异步，可 monkeypatch。
- `extension/login.js` — `chrome.identity.launchWebAuthFlow` 登录流程纯逻辑（可测）。
- `extension/login-test.html` — login.js 的回归测试页。
- `tests/test_v0_5_sessions.py`、`tests/test_v0_5_lark.py`、`tests/test_v0_5_auth.py`、`tests/test_v0_5_endpoints.py`。

**修改（后端）**
- `server/storage.py` — `init_db` 加 sessions 表迁移；导出 `_connect`/`_now`（已有）。
- `server/auth.py` — 重写：`Session` 模型 + `require_session` / `require_session_query` + `issue_state` / `consume_state`；删 `require_team*` / `_teams`。
- `server/app.py` — 加 `/auth/lark/login` `/auth/lark/callback` `/auth/me` `/auth/logout` `/auth/dev-login`；批注/presence/stream 端点换 `require_session*`、author 从 session 注入。
- `pyproject.toml` — `httpx` 从 dev 提升到 `dependencies`。
- `tests/conftest.py` — server fixture 换 env（`HG_AUTH_ALLOW_DEV=1`，去 `HG_TEAMS`）；page fixture 改注 `hg_session`。

**修改（扩展）**
- `extension/remote-store.js` — `authHeaders` 用 `session_token`，删 `X-User-*`。
- `extension/sync.js` — `team_token` → `session_token`，删 `X-User-*`。
- `extension/storage.js` — `configure` 读 `session_token`。
- `extension/content-script.js` — `_cfg.session_token` 透传；协同入口恢复。
- `extension/sidepanel.html` / `sidepanel.js` / `sidepanel.css` — 加「飞书登录」UI + 登录态展示 + 退出。
- `extension/manifest.json` — 加 `"identity"` 权限 + `host_permissions`。
- `static/annotate.js` / `static/viewer.html` — `hg_token` → `hg_session`；viewer 加 dev-login 入口（仅手动测）。

**修改（受影响的旧测试）**
- `tests/test_v0_4_auth.py`（重写为 session 模型）、`test_api.py`、`test_v0_4_broadcast.py`、`test_v0_4_delete.py`、`test_v0_4_presence.py`、`test_v0_4_sse.py`、`test_v0_4_schema.py`、`test_ui.py` — 换 session token。

---

## Task 1: sessions 存储（`server/sessions.py` + storage 迁移）

**Files:**
- Modify: `server/storage.py`（`init_db` 加表；`_connect`/`_now` 已存在供 import）
- Create: `server/sessions.py`
- Test: `tests/test_v0_5_sessions.py`

**Interfaces:**
- Produces: `sessions.create_session(open_id: str, name: str, team_id: str, ttl: int|None=None) -> str`；`sessions.get_session(token: str) -> dict|None`（键 `open_id`/`name`/`team_id`，过期返回 None）；`sessions.delete_session(token: str) -> bool`；`sessions.prune_expired() -> int`。

- [ ] **Step 1: 写失败测试 `tests/test_v0_5_sessions.py`**

```python
import time
from server import storage, sessions


def _init(tmp_path, monkeypatch):
    monkeypatch.setenv("HG_SESSION_TTL", "60")
    storage.init_db(tmp_path / "s.db")


def test_create_and_get(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    tok = sessions.create_session("ou_1", "alice", "team_a")
    assert isinstance(tok, str) and tok.startswith("sess_")
    s = sessions.get_session(tok)
    assert s == {"open_id": "ou_1", "name": "alice", "team_id": "team_a"}


def test_missing_returns_none(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    assert sessions.get_session("sess_nope") is None


def test_expired_returns_none(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    tok = sessions.create_session("ou_2", "bob", "team_a", ttl=1)
    time.sleep(1.2)
    assert sessions.get_session(tok) is None


def test_delete(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    tok = sessions.create_session("ou_3", "carol", "team_a")
    assert sessions.delete_session(tok) is True
    assert sessions.get_session(tok) is None
    assert sessions.delete_session(tok) is False


def test_prune(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    sessions.create_session("ou_4", "dan", "team_a", ttl=1)
    time.sleep(1.2)
    assert sessions.prune_expired() == 1
```

- [ ] **Step 2: 运行确认失败** — `uv run pytest tests/test_v0_5_sessions.py -v` → FAIL（模块不存在）。

- [ ] **Step 3: storage.py 加表迁移** — 在 `init_db` 的 `c.executescript(...)` 内追加（紧跟 annotations 表后）：

```sql
            CREATE TABLE IF NOT EXISTS sessions (
                token TEXT PRIMARY KEY,
                open_id TEXT,
                name TEXT,
                team_id TEXT,
                created_at TEXT,
                expires_at TEXT
            );
```

- [ ] **Step 4: 实现 `server/sessions.py`**

```python
from __future__ import annotations

import os
import secrets
from datetime import datetime, timezone

from .storage import _connect, _now

_DEFAULT_TTL = int(os.environ.get("HG_SESSION_TTL", "604800"))  # 7 天


def _expir(ttl: int) -> str:
    ts = datetime.now(timezone.utc).timestamp() + ttl
    return datetime.fromtimestamp(ts, timezone.utc).isoformat()


def create_session(open_id: str, name: str, team_id: str, ttl: int | None = None) -> str:
    token = "sess_" + secrets.token_hex(24)
    c = _connect()
    try:
        c.execute(
            "INSERT INTO sessions(token, open_id, name, team_id, created_at, expires_at) VALUES(?,?,?,?,?,?)",
            (token, open_id, name, team_id, _now(), _expir(ttl if ttl is not None else _DEFAULT_TTL)),
        )
    finally:
        c.close()
    return token


def get_session(token: str) -> dict | None:
    c = _connect()
    try:
        r = c.execute(
            "SELECT open_id, name, team_id, expires_at FROM sessions WHERE token=?", (token,)
        ).fetchone()
    finally:
        c.close()
    if r is None:
        return None
    exp = datetime.fromisoformat(r["expires_at"])
    if datetime.now(timezone.utc) > exp:
        return None
    return {"open_id": r["open_id"], "name": r["name"], "team_id": r["team_id"]}


def delete_session(token: str) -> bool:
    c = _connect()
    try:
        cur = c.execute("DELETE FROM sessions WHERE token=?", (token,))
        return cur.rowcount > 0
    finally:
        c.close()


def prune_expired() -> int:
    now = _now()
    c = _connect()
    try:
        cur = c.execute("DELETE FROM sessions WHERE expires_at < ?", (now,))
        return cur.rowcount
    finally:
        c.close()
```

> 注：`int | None` 在函数签名里配合文件顶 `from __future__ import annotations` 不会触发运行期求值，安全；pydantic 模型字段才必须用 `Optional`（见 Task 3）。

- [ ] **Step 5: 运行确认通过** — `uv run pytest tests/test_v0_5_sessions.py -v` → 5 PASS。

- [ ] **Step 6: commit**

```bash
git add server/sessions.py server/storage.py tests/test_v0_5_sessions.py
git commit -m "feat(v0.5-T1): sessions 表 + create/get/delete/prune"
```

---

## Task 2: 飞书 OpenAPI 客户端（`server/lark.py`）

**Files:**
- Create: `server/lark.py`
- Modify: `pyproject.toml`（httpx 提升到 runtime dependencies）
- Test: `tests/test_v0_5_lark.py`

**Interfaces:**
- Consumes: env `HG_LARK_APP_ID`/`HG_LARK_APP_SECRET`/`HG_LARK_BASE`/`HG_DEFAULT_TEAM`。
- Produces: `lark.authorize_url(redirect_uri, state) -> str`；`lark.exchange_code(code, redirect_uri) -> dict`（键 `open_id`/`name`/`team_id`）；内部 `app_access_token()` 带缓存。

- [ ] **Step 1: 写失败测试 `tests/test_v0_5_lark.py`**

```python
import json
import lark_testable  # 见下:把 httpx 注入点暴露便于 monkeypatch


def _env(monkeypatch):
    monkeypatch.setenv("HG_LARK_APP_ID", "cli_xxx")
    monkeypatch.setenv("HG_LARK_APP_SECRET", "sec_xxx")
    monkeypatch.setenv("HG_DEFAULT_TEAM", "team_default")


def test_authorize_url(monkeypatch):
    _env(monkeypatch)
    from server import lark
    u = lark.authorize_url("https://ext.chromiumapp.org/", "st123")
    assert u.startswith("https://open.feishu.cn/open-apis/authen/v1/authorize?")
    assert "app_id=cli_xxx" in u
    assert "state=st123" in u
    assert "redirect_uri=https" in u


def test_exchange_code(monkeypatch):
    _env(monkeypatch)
    calls = {}

    class FakeResp:
        def __init__(self, payload): self._p = payload
        status_code = 200
        def json(self): return self._p
        def raise_for_status(self): pass

    def fake_post(url, **kw):
        calls["post"] = (url, kw)
        if "app_access_token" in url:
            return FakeResp({"code": 0, "app_access_token": "aat", "expire": 7200})
        return FakeResp({"code": 0, "access_token": "uat", "open_id": "ou_9",
                         "name": "飞书用户", "tenant_key": "tk_1"})

    monkeypatch.setattr("server.lark.httpx.post", fake_post)
    from server import lark
    info = lark.exchange_code("code_abc", "https://ext.chromiumapp.org/")
    assert info == {"open_id": "ou_9", "name": "飞书用户", "team_id": "tk_1"}


def test_exchange_code_default_team(monkeypatch):
    _env(monkeypatch)

    class FakeResp:
        status_code = 200
        def __init__(self, p): self._p = p
        def json(self): return self._p
        def raise_for_status(self): pass

    def fake_post(url, **kw):
        if "app_access_token" in url:
            return FakeResp({"code": 0, "app_access_token": "aat", "expire": 7200})
        return FakeResp({"code": 0, "access_token": "uat", "open_id": "ou_9", "name": "u"})  # 无 tenant_key
    monkeypatch.setattr("server.lark.httpx.post", fake_post)
    from server import lark
    info = lark.exchange_code("code_abc", "https://ext.chromiumapp.org/")
    assert info["team_id"] == "team_default"
```

> 删掉测试里无用的 `import lark_testable` 行（残留占位）；实际靠 `monkeypatch.setattr("server.lark.httpx.post", ...)` 注入。

- [ ] **Step 2: 运行确认失败** — `uv run pytest tests/test_v0_5_lark.py -v` → FAIL（`server.lark` 不存在）。

- [ ] **Step 3: pyproject.toml 把 httpx 提升到运行时依赖**

把 `httpx>=0.28.1` 从 `[dependency-groups].dev` 移到 `[project].dependencies`（保留 dev 组里也行，但运行时必须有）。

- [ ] **Step 4: 实现 `server/lark.py`**

```python
from __future__ import annotations

import os
import time
import urllib.parse

import httpx

_BASE = None  # 懒读 env,方便测试 monkeypatch
_APP_TOKEN = {"token": None, "exp": 0.0}


def _base() -> str:
    return os.environ.get("HG_LARK_BASE", "https://open.feishu.cn")


def _app_id() -> str: return os.environ.get("HG_LARK_APP_ID", "")
def _app_secret() -> str: return os.environ.get("HG_LARK_APP_SECRET", "")


def authorize_url(redirect_uri: str, state: str) -> str:
    q = urllib.parse.urlencode({
        "app_id": _app_id(),
        "redirect_uri": redirect_uri,
        "state": state,
    })
    return f"{_base()}/open-apis/authen/v1/authorize?{q}"


def app_access_token() -> str:
    """带缓存的 app_access_token(默认 2h)。"""
    if _APP_TOKEN["token"] and time.time() < _APP_TOKEN["exp"]:
        return _APP_TOKEN["token"]
    r = httpx.post(
        f"{_base()}/open-apis/auth/v3/app_access_token/internal",
        json={"app_id": _app_id(), "app_secret": _app_secret()}, timeout=10,
    )
    r.raise_for_status()
    data = r.json()
    if data.get("code") != 0:
        raise RuntimeError(f"lark app_access_token failed: {data}")
    _APP_TOKEN["token"] = data["app_access_token"]
    _APP_TOKEN["exp"] = time.time() + min(int(data.get("expire", 7200)) - 60, 7200)
    return _APP_TOKEN["token"]


def _default_team() -> str:
    return os.environ.get("HG_DEFAULT_TEAM", "default")


def exchange_code(code: str, redirect_uri: str) -> dict:
    """code -> {open_id, name, team_id}。v1 access_token 端点直接回用户信息。"""
    r = httpx.post(
        f"{_base()}/open-apis/authen/v1/access_token",
        json={"grant_type": "authorization_code", "code": code, "app_access_token": app_access_token()},
        timeout=10,
    )
    r.raise_for_status()
    data = r.json()
    if data.get("code") != 0:
        raise RuntimeError(f"lark exchange failed: {data}")
    team_id = data.get("tenant_key") or data.get("tenant_id") or _default_team()
    return {"open_id": data["open_id"], "name": data.get("name", ""), "team_id": team_id}
```

- [ ] **Step 5: 运行确认通过** — `uv run pytest tests/test_v0_5_lark.py -v` → 3 PASS。

- [ ] **Step 6: commit**

```bash
git add server/lark.py pyproject.toml tests/test_v0_5_lark.py
git commit -m "feat(v0.5-T2): 飞书 OpenAPI 客户端(authorize_url/app_access_token/exchange_code)"
```

---

## Task 3: session 鉴权依赖 + state（`server/auth.py` 重写）

**Files:**
- Modify: `server/auth.py`（整体重写）
- Test: `tests/test_v0_5_auth.py`（替换旧 `test_v0_4_auth.py`）

**Interfaces:**
- Consumes: `sessions.get_session(token)`。
- Produces: `auth.Session`（pydantic，字段 `open_id`/`name`/`team_id`）；`require_session(authorization) -> Session`；`require_session_query(token) -> Session`；`issue_state() -> str`；`consume_state(state) -> bool`。

- [ ] **Step 1: 写失败测试 `tests/test_v0_5_auth.py`**

```python
import time
import pytest
from fastapi import HTTPException
from server import storage, sessions
from server.auth import require_session, require_session_query, issue_state, consume_state


def _init(tmp_path, monkeypatch):
    storage.init_db(tmp_path / "a.db")


def test_require_session_ok(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    tok = sessions.create_session("ou_1", "alice", "team_a")
    s = require_session(authorization=f"Bearer {tok}")
    assert s.open_id == "ou_1" and s.team_id == "team_a"


def test_require_session_missing(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    with pytest.raises(HTTPException) as e:
        require_session(authorization=None)
    assert e.value.status_code == 401


def test_require_session_bad(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    with pytest.raises(HTTPException) as e:
        require_session(authorization="Bearer sess_nope")
    assert e.value.status_code == 401


def test_require_session_query(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    tok = sessions.create_session("ou_2", "bob", "team_b")
    assert require_session_query(token=tok).team_id == "team_b"
    with pytest.raises(HTTPException) as e:
        require_session_query(token=None)
    assert e.value.status_code == 401


def test_state_roundtrip(monkeypatch):
    monkeypatch.setenv("HG_LARK_APP_SECRET", "sec_xxx")
    st = issue_state()
    assert consume_state(st) is True
    assert consume_state("garbage") is False
    assert consume_state(st) is True  # 无状态,可多次校验(回调一次性由调用方保证)


def test_state_expiry(monkeypatch):
    monkeypatch.setenv("HG_LARK_APP_SECRET", "sec_xxx")
    st = issue_state()
    # 篡改时间:把校验窗口调到未来 >300s
    import server.auth as a
    orig = a.time.time
    monkeypatch.setattr(a.time, "time", lambda: orig() + 400)
    assert consume_state(st) is False
```

- [ ] **Step 2: 运行确认失败** — `uv run pytest tests/test_v0_5_auth.py -v` → FAIL（`require_team_query` 等旧名/新名不符）。

- [ ] **Step 3: 重写 `server/auth.py`**

```python
"""Session 鉴权依赖 (v0.5 lark-oauth)。

- ``require_session``: ``Authorization: Bearer <token>`` -> ``Session``
- ``require_session_query``: ``?token=`` -> ``Session`` (SSE 用)
- ``issue_state``/``consume_state``: OAuth state 防 CSRF,HMAC 自签 + 5min TTL(无状态)
"""
from __future__ import annotations

import base64
import hashlib
import hmac
import json
import os
import time
from typing import Optional

from fastapi import Header, HTTPException, Query
from pydantic import BaseModel

from . import sessions


class Session(BaseModel):
    open_id: str
    name: str
    team_id: str


def _bearer(authorization: Optional[str]) -> str:
    return (authorization or "").removeprefix("Bearer ").strip()


def require_session(authorization: Optional[str] = Header(None)) -> Session:
    s = sessions.get_session(_bearer(authorization))
    if s is None:
        raise HTTPException(status_code=401, detail="invalid session")
    return Session(**s)


def require_session_query(token: Optional[str] = Query(None)) -> Session:
    # Query(None):缺 token 返 401 而非 422(与旧 require_team_query 一致)
    s = sessions.get_session((token or "").strip()) if token else None
    if s is None:
        raise HTTPException(status_code=401, detail="invalid session")
    return Session(**s)


_STATE_TTL = 300  # 秒


def _state_secret() -> bytes:
    return (os.environ.get("HG_LARK_APP_SECRET") or "dev-insecure-state-secret").encode()


def issue_state() -> str:
    body = json.dumps({"ts": int(time.time()), "n": os.urandom(8).hex()}, separators=(",", ":")).encode()
    sig = hmac.new(_state_secret(), body, hashlib.sha256).digest()
    return base64.urlsafe_b64encode(body).decode() + "." + base64.urlsafe_b64encode(sig).decode()


def consume_state(state: str) -> bool:
    try:
        body_b64, sig_b64 = state.split(".")
        body = base64.urlsafe_b64decode(body_b64)
        sig = base64.urlsafe_b64decode(sig_b64)
        if not hmac.compare_digest(sig, hmac.new(_state_secret(), body, hashlib.sha256).digest()):
            return False
        ts = json.loads(body)["ts"]
        return (int(time.time()) - ts) <= _STATE_TTL
    except Exception:
        return False
```

- [ ] **Step 4: 删旧 `tests/test_v0_4_auth.py`**（其断言基于 team-token，已被本任务测试取代；端点级隔离在 Task 5 重测）。

```bash
git rm tests/test_v0_4_auth.py
```

- [ ] **Step 5: 运行确认通过** — `uv run pytest tests/test_v0_5_auth.py -v` → 6 PASS。

- [ ] **Step 6: commit**

```bash
git add server/auth.py tests/test_v0_5_auth.py
git rm tests/test_v0_4_auth.py
git commit -m "feat(v0.5-T3): require_session 鉴权依赖 + HMAC state(替换 team-token)"
```

---

## Task 4: 鉴权端点（login/callback/me/logout/dev-login）

**Files:**
- Modify: `server/app.py`（加 5 个 `/auth/*` 端点；暂不动批注端点）
- Test: `tests/test_v0_5_endpoints.py`

**Interfaces:**
- Consumes: `lark.authorize_url`/`exchange_code`、`sessions.create_session`/`delete_session`、`auth.issue_state`/`consume_state`/`require_session`。
- Produces: `GET /auth/lark/login?redirect=` -> `{auth_url, state}`；`POST /auth/lark/callback {code, redirect_uri, state}` -> `{token, user:{id,name}, team_id}`；`GET /auth/me` -> `{id,name,team_id}`；`POST /auth/logout`；`POST /auth/dev-login {open_id,name,team?}`。

- [ ] **Step 1: 写失败测试 `tests/test_v0_5_endpoints.py`**

```python
import os
import pytest
from fastapi.testclient import TestClient
from server import storage, sessions
from server.app import app

client = TestClient(app)


def _init(tmp_path, monkeypatch, dev="1"):
    monkeypatch.setenv("HG_AUTH_ALLOW_DEV", dev)
    monkeypatch.setenv("HG_LARK_APP_ID", "cli_x")
    monkeypatch.setenv("HG_LARK_APP_SECRET", "sec_x")
    monkeypatch.setenv("HG_DEFAULT_TEAM", "team_d")
    storage.init_db(tmp_path / "e.db")


def test_lark_login_returns_authurl(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    r = client.get("/auth/lark/login", params={"redirect": "https://ext.chromiumapp.org/"})
    assert r.status_code == 200
    j = r.json()
    assert j["auth_url"].startswith("https://open.feishu.cn/open-apis/authen/v1/authorize?")
    assert "state" in j and j["state"]


def test_dev_login_and_me_and_logout(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    r = client.post("/auth/dev-login", json={"open_id": "ou_1", "name": "alice", "team": "team_a"})
    assert r.status_code == 200
    tok = r.json()["token"]
    assert r.json()["user"] == {"id": "ou_1", "name": "alice"}
    assert r.json()["team_id"] == "team_a"
    me = client.get("/auth/me", headers={"Authorization": f"Bearer {tok}"})
    assert me.status_code == 200 and me.json()["id"] == "ou_1"
    out = client.post("/auth/logout", headers={"Authorization": f"Bearer {tok}"})
    assert out.status_code == 200
    assert client.get("/auth/me", headers={"Authorization": f"Bearer {tok}"}).status_code == 401


def test_dev_login_disabled_in_prod(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch, dev="0")
    r = client.post("/auth/dev-login", json={"open_id": "ou_1", "name": "alice"})
    assert r.status_code == 404


def test_callback(monkeypatch, tmp_path):
    _init(tmp_path, monkeypatch)
    monkeypatch.setattr("server.lark.exchange_code",
                        lambda code, redir: {"open_id": "ou_9", "name": "飞书", "team_id": "tk_1"})
    r = client.post("/auth/lark/callback",
                    json={"code": "c", "redirect_uri": "https://ext.chromiumapp.org/", "state": "STALE"})
    # state 必须先从 /auth/lark/login 拿到;这里先取一个合法 state
    st = client.get("/auth/lark/login", params={"redirect": "https://ext.chromiumapp.org/"}).json()["state"]
    r = client.post("/auth/lark/callback",
                    json={"code": "c", "redirect_uri": "https://ext.chromiumapp.org/", "state": st})
    assert r.status_code == 200
    j = r.json()
    assert j["user"] == {"id": "ou_9", "name": "飞书"} and j["team_id"] == "tk_1"
    assert j["token"].startswith("sess_")


def test_me_requires_session(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    assert client.get("/auth/me").status_code == 401
```

- [ ] **Step 2: 运行确认失败** — `uv run pytest tests/test_v0_5_endpoints.py -v` → FAIL（路由不存在）。

- [ ] **Step 3: `server/app.py` 加端点** — 顶部 import 调整：

```python
from . import lark, sessions, storage
from .auth import Session, consume_state, issue_state, require_session
```

在 `/health` 之前或之后插入（保留现有批注端点不动，Task 5 再改）：

```python
from pydantic import BaseModel as _BM


class CallbackIn(_BM):
    code: str
    redirect_uri: str
    state: str


class DevLoginIn(_BM):
    open_id: str
    name: str
    team: Optional[str] = None


@app.get("/auth/lark/login")
def lark_login(redirect: str):
    state = issue_state()
    return {"auth_url": lark.authorize_url(redirect, state), "state": state}


@app.post("/auth/lark/callback")
def lark_callback(payload: CallbackIn):
    if not consume_state(payload.state):
        raise HTTPException(status_code=400, detail="bad state")
    try:
        info = lark.exchange_code(payload.code, payload.redirect_uri)
    except Exception as e:
        raise HTTPException(status_code=502, detail=f"lark exchange failed: {e}")
    token = sessions.create_session(info["open_id"], info["name"], info["team_id"])
    return {"token": token, "user": {"id": info["open_id"], "name": info["name"]}, "team_id": info["team_id"]}


@app.get("/auth/me")
def auth_me(session: Session = Depends(require_session)):
    return {"id": session.open_id, "name": session.name, "team_id": session.team_id}


@app.post("/auth/logout")
def auth_logout(session: Session = Depends(require_session)):
    # session 不带 token;用 header 原值删
    return {"ok": True}  # 见下方说明:实际删行靠 require_session 已校验有效,这里补删


@app.post("/auth/dev-login")
def dev_login(payload: DevLoginIn):
    if os.environ.get("HG_AUTH_ALLOW_DEV") != "1":
        raise HTTPException(status_code=404, detail="dev login disabled")
    team = payload.team or os.environ.get("HG_DEFAULT_TEAM", "default")
    token = sessions.create_session(payload.open_id, payload.name, team)
    return {"token": token, "user": {"id": payload.open_id, "name": payload.name}, "team_id": team}
```

`auth_logout` 需要删 sessions 行；`require_session` 只返回 `Session`（不含 token）。改：让 `auth_logout` 接 `authorization: Optional[str] = Header(None)` 直接删：

```python
@app.post("/auth/logout")
def auth_logout(session: Session = Depends(require_session), authorization: Optional[str] = Header(None)):
    token = (authorization or "").removeprefix("Bearer ").strip()
    if token:
        sessions.delete_session(token)
    return {"ok": True}
```

并确保 `app.py` 顶部已有 `from typing import Optional` 与 `from fastapi import Header`（现有 import 已有 `Header`）。

- [ ] **Step 4: 运行确认通过** — `uv run pytest tests/test_v0_5_endpoints.py -v` → 5 PASS。

- [ ] **Step 5: commit**

```bash
git add server/app.py tests/test_v0_5_endpoints.py
git commit -m "feat(v0.5-T4): /auth/lark/login|callback + /me + /logout + /dev-login"
```

---

## Task 5: 批注/presence/stream 端点切到 session + 更新受影响测试

**Files:**
- Modify: `server/app.py`（create/list/delete annotation、presence、stream）
- Test: 更新 `tests/test_api.py`、`test_v0_4_broadcast.py`、`test_v0_4_delete.py`、`test_v0_4_presence.py`、`test_v0_4_sse.py`、`test_v0_4_schema.py`

**Interfaces:**
- `POST /api/annotations`：`session: Session = Depends(require_session)`；`payload.author = {"id": session.open_id, "name": session.name}`；`team_id = session.team_id`。**删** `x_user_id`/`x_user_name` 参数。
- `DELETE /api/annotations/{aid}`：`storage.delete_annotation(aid, session.team_id, session.open_id)`。
- `POST /api/presence`：`PresenceIn` 改 `{doc, op}`；user 由 session 组装。
- `GET /api/stream`：`session: Session = Depends(require_session_query)`。

- [ ] **Step 1: 改 `server/app.py` 端点**

```python
@app.post("/api/annotations")
async def create_annotation(payload: AnnotationCreate, session: Session = Depends(require_session)):
    payload.author = {"id": session.open_id, "name": session.name}
    ann = storage.save_annotation(payload, team_id=session.team_id)
    await rooms.broadcast(session.team_id, payload.document_id, "annotation:created", ann)
    return ann


@app.get("/api/annotations")
def list_annotations(document_id: str, session: Session = Depends(require_session)):
    return {"items": storage.list_annotations(document_id, session.team_id)}


@app.delete("/api/annotations/{aid}")
async def delete_annotation(aid: str, session: Session = Depends(require_session)):
    try:
        deleted = storage.delete_annotation(aid, session.team_id, session.open_id)
    except PermissionError:
        raise HTTPException(status_code=403, detail="not owner")
    for d in deleted:
        await rooms.broadcast(session.team_id, d["document_id"], "annotation:deleted", {"id": d["id"]})
    return {"ok": True, "deleted": [d["id"] for d in deleted]}
```

Presence：把 `PresenceIn` 改成 `{doc, op}`，端点用 session：

```python
class PresenceIn(BaseModel):
    doc: str
    op: str = "join"


@app.post("/api/presence")
async def presence_post(payload: PresenceIn, session: Session = Depends(require_session)):
    user = {"id": session.open_id, "name": session.name}
    await presence_update(session.team_id, payload.doc, user, payload.op)
    return {"ok": True}
```

stream：`team_id: str = Depends(require_team_query)` → `session: Session = Depends(require_session_query)`，函数体里 `team_id` 全换 `session.team_id`。

- [ ] **Step 2: 写一个端点级 session 测试** — 在 `tests/test_v0_5_endpoints.py` 追加：

```python
def test_annotation_uses_session_identity(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    from server.models import DocumentCreate
    storage.register_document(DocumentCreate(document_id="doc_x"))
    tok = sessions.create_session("ou_a", "alice", "team_a")
    H = {"Authorization": f"Bearer {tok}"}
    r = client.post("/api/annotations",
                    json={"document_id": "doc_x", "selector": {"type": "TextQuoteSelector", "exact": "hi"}, "quote": "hi", "author": {"id": "forged", "name": "x"}},
                    headers=H)
    assert r.status_code == 200
    assert r.json()["author"] == {"id": "ou_a", "name": "alice"}  # 来自 session,非 body
    assert r.json()["team_id"] == "team_a"


def test_delete_non_owner_403(tmp_path, monkeypatch):
    _init(tmp_path, monkeypatch)
    from server.models import DocumentCreate
    storage.register_document(DocumentCreate(document_id="doc_x"))
    a = sessions.create_session("ou_a", "alice", "team_a")
    b = sessions.create_session("ou_b", "bob", "team_a")
    created = client.post("/api/annotations",
                          json={"document_id": "doc_x", "selector": {"type": "TextQuoteSelector", "exact": "hi"}, "quote": "hi"},
                          headers={"Authorization": f"Bearer {a}"}).json()
    d = client.delete(f"/api/annotations/{created['id']}", headers={"Authorization": f"Bearer {b}"})
    assert d.status_code == 403
```

- [ ] **Step 3: 更新受影响测试** — 给每个文件加一个 mint 辅助并替换 `Bearer tok_*`：

每个文件顶部加：
```python
from server import storage, sessions
def _sess(open_id, name, team):
    return sessions.create_session(open_id, name, team)
```
把 `headers={"Authorization": "Bearer tok_a", "X-User-Id": "u1", "X-User-Name": "user_one"}` 形式替换为 `headers={"Authorization": "Bearer " + _sess("u1", "user_one", "team_a")}`；`init` 里去掉 `HG_TEAMS` 的 `monkeypatch.setenv`，保留 `storage.init_db` + `register_document`。`sync.js`/SSE 的 `?token=tok_a` 换成 `?token=` + `_sess(...)`。

- [ ] **Step 4: 运行全量** — `uv run pytest -q` → 受影响测试转绿（可能有零星断言差异，按实际改）。

- [ ] **Step 5: commit**

```bash
git add server/app.py tests/
git commit -m "feat(v0.5-T5): 批注/presence/stream 切到 require_session,author/team 从 session 注入"
```

---

## Task 6: conftest + viewer 迁移 + 修剩余 UI 测试

**Files:**
- Modify: `tests/conftest.py`、`static/annotate.js`、`static/viewer.html`、`tests/test_ui.py`（及其它经 page fixture 的测试）

- [ ] **Step 1: conftest server fixture 换 env**

```python
    env["HG_AUTH_ALLOW_DEV"] = "1"
    env["HG_LARK_APP_ID"] = "cli_test"
    env["HG_LARK_APP_SECRET"] = "sec_test"
    env["HG_DEFAULT_TEAM"] = "team_test"
    # 删掉 env["HG_TEAMS"] = ...
```

- [ ] **Step 2: page fixture 注 hg_session**

```python
@pytest.fixture
def page(browser, server):
    import httpx, json as _json
    pg = browser.new_page()
    tok = httpx.post(f"{server}/auth/dev-login",
                     json={"open_id": "ou_test", "name": "测试", "team": "team_test"}).json()["token"]
    pg.add_init_script(f"try{{localStorage.setItem('hg_session',{_json.dumps(tok)})}}catch(e){{}}")
    yield pg
    pg.close()
```

- [ ] **Step 3: `static/annotate.js`** — `hg_token` → `hg_session`（读 localStorage 的键名 + 注入 `Authorization`）。`viewer.html` 加一个「dev 登录」按钮（仅手动测用，点了调 `/auth/dev-login` 存 `hg_session`）。

- [ ] **Step 4: 运行 `uv run pytest -q`** → 全绿（含 test_ui / test_relocate 等经 page fixture 的）。

- [ ] **Step 5: commit**

```bash
git add tests/conftest.py static/annotate.js static/viewer.html tests/test_ui.py
git commit -m "feat(v0.5-T6): conftest 走 dev-login + viewer 用 session token"
```

---

## Task 7: 扩展客户端切 session_token

**Files:**
- Modify: `extension/remote-store.js`、`extension/sync.js`、`extension/storage.js`、`extension/content-script.js`

**Interfaces:** `cfg.session_token` 取代 `cfg.team_token`；不再发 `X-User-Id`/`X-User-Name`。

- [ ] **Step 1: `remote-store.js` `authHeaders`**

```js
  function authHeaders(cfg) {
    return {
      "Authorization": "Bearer " + cfg.session_token,
      "Content-Type": "application/json",
    };
  }
```
删 `user`/`X-User-Id`/`X-User-Name`。`buildCreateRequest` 不变（用 authHeaders）。

- [ ] **Step 2: `sync.js`** — `var teamToken = opts.team_token` → `var sessionToken = opts.session_token`；URL `&token=` + `encodeURIComponent(sessionToken)`；`sendPresence` 的 `Authorization: Bearer ` + sessionToken，删 `X-User-*` 头；`bye` fetch 同理。

- [ ] **Step 3: `storage.js` `configure`** — 读 `cfg.session_token`（字段名透传，无需改 configure 逻辑，只要调用方传对）。

- [ ] **Step 4: `content-script.js`** — `chrome.storage.sync.get(["mode","backend","session_token","user"], ...)`；`Storage.configure(_cfg)` 与 `Sync.start({..., session_token: _cfg.session_token, ...})` 透传 session_token。

- [ ] **Step 5: 更新现有扩展测试页** — `remote-store-test.html` / `sync-test.html` 里把 `team_token` 改 `session_token`，断言 header 无 `X-User-*`。

- [ ] **Step 6: 运行 `uv run pytest tests/test_plugin_pages.py -v`** → 绿。

- [ ] **Step 7: commit**

```bash
git add extension/remote-store.js extension/sync.js extension/storage.js extension/content-script.js extension/*-test.html
git commit -m "feat(v0.5-T7): 扩展客户端 team_token -> session_token,去 X-User 头"
```

---

## Task 8: 侧边栏飞书登录 UI + manifest

**Files:**
- Create: `extension/login.js`、`extension/login-test.html`
- Modify: `extension/sidepanel.html`、`extension/sidepanel.js`、`extension/sidepanel.css`、`extension/manifest.json`
- Test: `tests/test_plugin_pages.py` NAMES 加 `login-test.html`

**Interfaces:** `Login.start({backend}) -> Promise<{token,user,team_id}>`（内部 `getRedirectURL` → `/auth/lark/login` → `launchWebAuthFlow` → 解析 code → `/auth/lark/callback`）。

- [ ] **Step 1: 写 `extension/login-test.html`**（断言 `Login.parseCallbackUrl(url)` 能从重定向 URL 取 `code`/`state`；`Login.buildCallbackBody({code,redirect,state})` 正确）。

- [ ] **Step 2: 实现 `extension/login.js`**

```js
window.Login = (function () {
  function parseCallbackUrl(url) {
    var u = new URL(url);
    return { code: u.searchParams.get("code") || "", state: u.searchParams.get("state") || "" };
  }
  function buildCallbackBody(o) {
    return JSON.stringify({ code: o.code, redirect_uri: o.redirect, state: o.state });
  }
  async function start(opts) {
    opts = opts || {};
    var backend = opts.backend || "";
    var redirect = chrome.identity.getRedirectURL();
    var lr = await fetch(backend + "/auth/lark/login?redirect=" + encodeURIComponent(redirect)).then(function (r) { return r.json(); });
    var respUrl = await chrome.identity.launchWebAuthFlow({ url: lr.auth_url, interactive: true });
    var cb = parseCallbackUrl(respUrl);
    var r = await fetch(backend + "/auth/lark/callback", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: buildCallbackBody({ code: cb.code, redirect: redirect, state: cb.state }),
    });
    if (!r.ok) throw new Error("login failed " + r.status);
    return r.json();
  }
  return { start: start, parseCallbackUrl: parseCallbackUrl, buildCallbackBody: buildCallbackBody };
})();
```

- [ ] **Step 3: `sidepanel.html`** 在 `#header` 下加协同登录区：

```html
<div id="collab">
  <input id="backend-input" placeholder="后端地址 https://…">
  <button id="lark-login-btn">飞书登录</button>
  <span id="login-state"></span>
</div>
```

- [ ] **Step 4: `sidepanel.js`** — 登录按钮逻辑：读 backend → `Login.start` → `chrome.storage.sync.set({mode:"synced", backend, session_token: token, user})` → 刷新；启动时若有 session_token 调 `/auth/me` 校验，失效提示重登；显示用户名 + 「退出」(调 `/auth/logout` 并清 storage)。

- [ ] **Step 5: `sidepanel.css`** — 给 `#collab` / `#lark-login-btn` 暖色样式（沿用 `--primary` 等 token）。

- [ ] **Step 6: `manifest.json`**

```json
  "permissions": ["activeTab", "sidePanel", "storage", "identity"],
  "host_permissions": ["https://YOUR_BACKEND_HOST/*"],
```

`sidepanel.html` scripts 加 `login.js`（在 `sidepanel.js` 前）。

- [ ] **Step 7: `test_plugin_pages.py` NAMES** 加 `"login-test.html"`。运行 `uv run pytest tests/test_plugin_pages.py -v` → 绿。

- [ ] **Step 8: commit**

```bash
git add extension/login.js extension/login-test.html extension/sidepanel.* extension/manifest.json tests/test_plugin_pages.py
git commit -m "feat(v0.5-T8): 侧边栏飞书登录 UI + manifest(identity/host_permissions)"
```

---

## Task 9: 文档 + 全量回归 + 收尾

**Files:**
- Modify: `README.md`、`RELEASE_NOTES.md`、`DEVELOPMENT.md`、`docs/2026-07-05-v0.4-deploy.md`（v0.5 env）

- [ ] **Step 1: README** — 「最近更新」加 v0.5 条目（协同开放 + 飞书登录），drop v0.4（保持 3 版：v0.5/v0.4.2/v0.4.1）；改「已知限制」里「协同暂未开放」为已开放。
- [ ] **Step 2: RELEASE_NOTES** — 顶部加 v0.5。
- [ ] **Step 3: DEVELOPMENT** — env 表加 `HG_LARK_*` / `HG_AUTH_ALLOW_DEV` / `HG_SESSION_TTL`；删 `HG_TEAMS`；架构/路线图更新。
- [ ] **Step 4: deploy 文档** — v0.5 env + manifest host_permissions + 飞书后台重定向 URI 说明。
- [ ] **Step 5: 全量 `uv run pytest -v`** → 全绿。
- [ ] **Step 6: commit**

```bash
git add README.md RELEASE_NOTES.md DEVELOPMENT.md docs/2026-07-05-v0.4-deploy.md
git commit -m "docs(v0.5-T9): README/RELEASE_NOTES/DEVELOPMENT/deploy 更新飞书 OAuth"
```

---

## Self-Review

**1. Spec coverage：** sessions(T1)/lark(T2)/auth依赖+state(T3)/login·callback·me·logout·dev-login(T4)/批注端点切session(T5) = spec §5 全覆盖；扩展 login UI + remote-store/sync/storage/content-script/manifest(T7-T8) = §6 全覆盖；env(§7)/安全(§8 state/secret/dev-gate/硬身份)/测试(§9)/迁移(§10 HG_TEAMS 下线 + viewer)。✓
**2. 占位符：** 已删除 Task 2 测试里残留的 `import lark_testable` 占位说明；所有 step 含真实代码。✓
**3. 类型一致：** `Session{open_id,name,team_id}`、`sessions.create_session(open_id,name,team_id,ttl)`、`lark.exchange_code->dict{open_id,name,team_id}`、`cfg.session_token` 跨任务一致。✓

**飞书端点注**：本计划用 v1 `/authen/v1/authorize` + `/authen/v1/access_token`；飞书另有 v2 端点，真实联调时若 v1 不可用，改 `lark.py` 端点字符串即可（流程不变），属可变设计方向（见交付报告）。
