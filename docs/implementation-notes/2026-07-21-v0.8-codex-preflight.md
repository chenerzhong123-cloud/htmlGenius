# v0.8 Codex App Server Adapter · Preflight（2026-07-21）

> 施工前对齐。记录真实入口、Codex App runtime 发现/schema/handshake 结果、Spec 等价适配；≤80 行。

## Night Pack A 前置（已通过）
- candidate 闭环：`bridge/candidate-workspace.mjs`（snapshot 0400/manifest 0600/sibling 原子/形态校验）、`host-runner.mjs executeCandidateRun`、`background.js completeCandidate`（逐字段比对→new_artifact）、`content-script.js handleArtifactUpdateReady`（new_artifact 受控消费）。
- 真实 Claude smoke 已端到端验证（2026-07-21）。

## Native Host 真实结构（v0.8 复用点）
- `background.js`：`bridge-start`（provider/run_kind 透传）→ `connectNative(com.htmlgenius.local_bridge)` → `claude_handoff_start`；`onHostMessage`（candidate-ready）→ `completeCandidate`。
- Storage facade：getBridgeRun/saveBridgeRun/updateBridgeRun/getActiveBridgeRunForTab/getLatestCompletedCandidateRun/saveBridgeSession/getBridgeSession。
- run record 字段：run_id/logical_document_id/tab_id/provider/session_mode/run_kind/task_sha256/base_artifact_hash/status/candidate_uri/candidate_sha256/manifest_path。
- session record 字段：logical_document_id/provider/ownership/session_id/workspace_path/status。
- `task-bundle.mjs buildCandidatePrompt`（前言+renderPrompt）；v0.8 的 codex 前言等价复用此模式。

## Codex App runtime 发现与信任（spec §2.2，全部通过）
- App：`/Applications/ChatGPT.app`；`CFBundleIdentifier=com.openai.codex`；App 版本 26.715.61943。
- runtime：`Contents/Resources/codex`（regular executable，265 MB）；版本 `codex-cli 0.145.0-alpha.27`。
- codesign：`Identifier=codex`，`TeamIdentifier=2DC432GLL2`（spec 要求）✓。
- PATH 的 `codex` 是无关博客工具 `codex-cli@0.1.3`；spec §2.1 明确禁用，不发现/执行。

## Schema generation + 协议（spec §2）
- `<runtime> app-server generate-json-schema --out <tmp>` 成功；产出 ClientRequest/ServerNotification/ServerRequest + v1/v2 完整 schema。
- allowed：`initialize`/`initialized`/`thread/start`/`thread/resume`/`turn/start`；terminal event = `turn/completed`。
- forbidden（client 不发）：`thread/list`/`thread/read`/`thread/fork`/`turn/steer`/`thread/archive`/`delete`/`rollback` 等。
- `turn/start` 支持 `cwd`/`cwds` + `sandbox`/`sandbox_workspace_write`/`sandbox_mode` + `approval_policy`/`approval_mode`（§6.3 三能力可满足）。

## Handshake（真实跑通）
- framing：**newline-delimited JSON-RPC**（每条 JSON 一行，非 LSP Content-Length）。
- `initialize` params：`{clientInfo:{name,version}, protocolVersion:1, capabilities:{}}` → result{userAgent,codexHome,platformFamily}。
- 发 `initialized`（notification）后 server 主动推 notification（`remoteControl/status/changed`）。

## Spec 与代码差异 → 等价适配
1. App 发现：首发查 `/Applications/ChatGPT.app` + `~/Applications/ChatGPT.app`；兜底用 `mdfind "kMDItemCFBundleIdentifier == 'com.openai.codex'"`（本机命中 /Applications/ChatGPT.app）。
2. workspace：codex 用 `.htmlgenius-bridge/codex/<logicalId>/runs/<runId>`（与 claude 的 provider 子目录并列）。
3. handshake framing 真实 = newline-delimited（client 按此实现，不猜 LSP）。
4. protocolVersion 用 1（v1）；schema 有 v2，首发 v1。

## Gate 判定：通过，进入施工。
